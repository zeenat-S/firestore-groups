import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'firestore-groups',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
